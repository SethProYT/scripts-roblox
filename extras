local library = loadstring(game:HttpGet(('https://raw.githubusercontent.com/AikaV3rm/UiLib/master/Lib.lua')))()

local w = library:CreateWindow("Boxing League") -- Creates the window

local b = w:CreateFolder("Main") -- Creates the folder(U will put here your buttons,etc)

b:Label("Fixed some bugs :)",{
    TextSize = 25; -- Self Explaining
    TextColor = Color3.fromRGB(255,255,255); -- Self Explaining
    BgColor = Color3.fromRGB(69,69,69); -- Self Explaining
    
}) 

b:Toggle("Legit No-Slow",function(bool)
    noslow = bool
    print(shared.toggle)
end)

b:Toggle("Hacker Bo1 No-Slow",function(bool)
    nonlegit = bool
    print(shared.toggle)
    if not nonlegit then
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 16
    end
end)

b:DestroyGui()

--[[
How to refresh a dropdown:
1)Create the dropdown and save it in a variable
local yourvariable = b:Dropdown("Hi",yourtable,function(a)
    print(a)
end)
2)Refresh it using the function
yourvariable:Refresh(yourtable)
How to refresh a label:
1)Create your label and save it in a variable
local yourvariable = b:Label("Pretty Useless NGL",{
    TextSize = 25; -- Self Explaining
    TextColor = Color3.fromRGB(255,255,255);
    BgColor = Color3.fromRGB(69,69,69);
})
2)Refresh it using the function
yourvariable:Refresh("Hello") It will only change the text ofc
]]





function legit()
    spawn(function()
        while noslow == true do
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 16
            wait()
        end
    end)
end



function hacker()
    spawn(function()
        while nonlegit == true do
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 90
        end
    end)
end